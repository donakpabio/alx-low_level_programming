#include <string.h>
#include "main.h"

/**
 * strstr - Do something
 * @haystack: input
 * @needle: input
 * Return: result
 */
char *_strstr(char *haystack, char *needle)
{
	return (strstr(haystack, needle));
}

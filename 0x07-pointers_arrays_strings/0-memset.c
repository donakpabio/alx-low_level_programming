#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * _memset - Do something
 * @s: input
 * @b: input
 * @n: input
 * Return: Result
 */
char *_memset(char *s, char b, unsigned int n)
{
	return (memset(s, b, n));
}

#include <string.h>
#include <stdio.h>
#include "main.h"

/**
 * _memcpy - Do something
 * @dest: input
 * @src: input
 * @n: input
 * Return: result
 */
char *_memcpy(char *dest, char *src, unsigned int n)
{
	return (memcpy(dest, src, n));
}

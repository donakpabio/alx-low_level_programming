#include <string.h>
#include "main.h"

/**
 * _strncpy - Copy
 * @dest: input
 * @src: input
 * @n: input
 * Return: result
 */
char *_strncpy(char *dest, char *src, int n)
{
	return (strncpy(dest, src, n));
}
